services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: ai_agile
    ports:
      - "5432:5432"
    volumes:
      - ai_agile_pg:/var/lib/postgresql/data

volumes:
  ai_agile_pg:
